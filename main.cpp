#include "graph.cpp"
#include <bits/stdc++.h>

using namespace std;

template <typename T>

T add(T a, T b) {
    return a + b;
}

template <typename T>

class Node {
public:
    Node(int v, T t) {
    }
};

class Graph {
private:
    int x;

public:
    Graph() {
    }
};

int main() {
    // +
    Node n = Node<int>(10, 21);
    cout << "hello there" << endl;
    Graph t = Graph();
}
